#include <stdio.h>
#include <conio.h>
int main()
{
	int a, b, c, t;

{
	printf("\nEnter the Multiplicand :");
    scanf("%d", &a);

	printf("\nEnter Multiplier :");
    scanf("%d", &t);
    printf("\n");

	b=0;
}
	for(c=a; c<=(a*t); c+=a)
{
    b++;
	printf("%d x %d = %d\n", a, b, c);
}
    getch();
    return 0;
}
